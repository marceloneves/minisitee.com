## 15:44 — Publicação da seção com vídeo na prática

**O que mudou:** `git push` para o `main` com a seção "Como criar um minisitee na prática" (vídeo vertical e texto de apoio); a VPS faz o deploy automático.

**Arquivos:** —

**Commit:** — (publicados `0551a3c` a `8644c73`)

**Motivo:** colocar a seção nova no ar.
