## 11:45 — Sobe o servidor local na porta 8001

**O que mudou:** nada no código. Subi o servidor local
(`python3 -m http.server`) para conferir a landing no navegador.

**Arquivos:** —

**Commit:** —

**Motivo:** a porta 8000 padrão está ocupada por outro projeto
(`guinchorj.com`), então a landing ficou em `http://localhost:8001`.

---

## 11:53 — Reescreve a chamada do hero falando com o visitante

**O que mudou:** a chamada do hero, que listava recursos em terceira pessoa
("Catálogo com foto e preço, botão de WhatsApp..."), passou a falar direto
com quem acessa: "Seus clientes veem suas fotos e preços, falam com você no
WhatsApp e acham seu endereço — tudo em um link só na bio. Você monta hoje,
sem site e sem código." A `og:description` acompanhou o novo texto.

**Arquivos:** `index.html`

**Commit:** —

**Motivo:** pedido de deixar o texto conversando com o usuário que acessa o
site, em vez de só enumerar funcionalidades.

---

## 12:02 — Troca a chamada do hero por uma que abre pela dor

**O que mudou:** a versão das 11:53 não agradou. A chamada passou a abrir
pelo problema de quem vende no direct: "Chega de mandar preço um por um no
direct. Suas fotos, preços, WhatsApp, horário e endereço em um link só na
bio. Pronto hoje, sem site e sem código." A `og:description` acompanhou.

**Arquivos:** `index.html`

**Commit:** —

**Motivo:** entre quatro direções apresentadas (dor, tom direto no "você",
abertura com pergunta, ou voltar ao texto original), a escolhida foi a que
começa pela dor.

---

## 12:05 — Fecha a chamada do hero com "em 3 minutos e sem programador"

**O que mudou:** o fecho "Pronto hoje, sem site e sem código" virou "Você
mesmo cria seu minisitee, em 3 minutos e sem programador", texto escolhido
pelo Marcelo. A `og:description` acompanhou.

**Arquivos:** `index.html`

**Commit:** `645a94a`

**Motivo:** o fecho antigo repetia os selos logo abaixo ("Pronto em
minutos") e não dizia quem faz o trabalho.

---

## 12:05 — Põe a seção "O que é" na linguagem do resto da landing

**O que mudou:** a seção fugia do padrão das outras em três pontos, todos
corrigidos. O header não tinha `sub` e ganhou um; o bloco `.definicao`, que
era uma citação com barra preta de 3px e texto de 23px (tratamento que não
existe em nenhum outro lugar da página), virou três cartões no mesmo padrão
dos `.passo`/`.bloco` — "Não é loja virtual", "Não é site" e "É o meio do
caminho"; e as cenas da jornada viraram cartões com a tela do celular numa
bandeja cinza e a legenda dentro, no lugar das legendas soltas. A `.jornada`
passou de flex para grid `1fr auto 1fr auto 1fr`, o que dispensou o
`margin-top: -46px` que centralizava as setas na marra.

**Arquivos:** `index.html`, `styles.css`

**Commit:** `645a94a`

**Motivo:** a seção destoava visualmente do resto da landing.

**Detalhe:** a tela do WhatsApp (`.mini-zap`) era cinza e sumiria na bandeja
cinza — passou a branca, e o balão de entrada assumiu o cinza para continuar
se distinguindo. Os mocks ganharam um nível de indentação por causa da
`.cena-tela` nova.

---

## 12:05 — Sobe o limite do plano Free de 5 para 6 itens (só o texto)

**O que mudou:** os três textos que citavam o limite passaram a dizer 6
itens: o JSON-LD de preços e o cartão do plano Free no `index.html`, e a
cláusula do plano Free nos termos.

**Arquivos:** `index.html`, `termos/index.html`

**Commit:** `645a94a`

**Motivo:** pedido de subir o limite gratuito para 6 itens.

**Atenção:** vale a mesma ordem registrada em 11/09, quando o limite subiu
de 3 para 5 — primeiro a migration no banco, depois o app, depois a landing.
Quem segura o limite de verdade é o trigger no banco; se a landing prometer
6 antes, o sexto item dá erro. A mudança no `app.minisitee.com` ainda não
foi feita.

---

## 12:05 — Reescreve os títulos das seções do problema e dos passos

**O que mudou:** "Vender só pelo direct custa caro" virou "No direct, a
venda esfria", com o subtítulo trocado por "O cliente pergunta o preço às
22h, você responde no dia seguinte — e aí ele já comprou de outro".
"Três passos, um café" virou "Três passos, três minutos".

**Arquivos:** `index.html`

**Commit:** `645a94a`

**Motivo:** os dois títulos não agradaram. O novo da seção de passos também
casa com os "3 minutos" prometidos no hero e no CTA final.

---

## 12:20 — Reescreve a terceira carta da seção "O que é"

**O que mudou:** "É o meio do caminho / O pedaço que faltava entre o seu
post no Instagram e a conversa no WhatsApp" virou "É a sua vitrine / Foto,
preço, horário e endereço numa página só. O cliente chega no WhatsApp já
sabendo o que quer".

**Arquivos:** `index.html`

**Commit:** `a9c745a`

**Motivo:** "meio do caminho" soava a coisa pela metade e "o pedaço que
faltava" não dizia o que o produto é. Depois de duas cartas que negam
("não é loja virtual", "não é site"), a terceira precisa afirmar algo
concreto.

---

## 22:40 — Adiciona os blocos QR code e Agenda

**O que mudou:** a seção "Blocos" ganhou os cartões "QR code" e "Agenda",
cada um com ícone e descrição, e os dois entraram no `featureList` dos
dados estruturados.

**Arquivos:** `index.html`

**Commit:** `0749a3d`

**Motivo:** o aplicativo passou a oferecer esses dois tipos de item
(commit `0122125` do app.minisitee.com) e a landing ainda listava só os
onze antigos.

---

## 22:42 — Ordena os blocos em ordem alfabética

**O que mudou:** os 13 cartões da seção "Blocos" passaram a aparecer em
ordem alfabética, de "Agenda" a "Redes sociais". O cartão "E o resto?"
continua por último.

**Arquivos:** `index.html`

**Commit:** `348e1c8`

**Motivo:** com os tipos novos a lista cresceu, e a ordem alfabética é a
mesma que o seletor de tipos do aplicativo passou a usar.

---

## 22:42 — Abre em nova aba os links que saem da landing

**O que mudou:** os 14 links para o aplicativo e para as páginas de
termos, privacidade e cookies ganharam `target="_blank" rel="noopener"`.
O formulário do endereço no hero também passou a abrir o aplicativo em
nova aba (`window.open` no `script.js`), e o `script.js` ganhou `?v=` na
chamada para o navegador não usar a versão antiga guardada. Ficaram de
fora as âncoras internas (`#blocos`, `#precos`...) e os links `mailto:`.

**Arquivos:** `index.html`, `script.js`

**Commit:** `fa6fbd6`

**Motivo:** pedido para que os links da landing abram em nova página, sem
tirar o visitante do site. As âncoras só rolam a própria página e, em nova
aba, perderiam a função; os `mailto:` abririam uma aba vazia.

---

## 22:44 — Adiciona o item "Início" no menu do topo

**O que mudou:** o menu principal ganhou "Início" como primeiro item,
apontando para `#topo` (o hero).

**Arquivos:** `index.html`

**Commit:** `1e66672`

**Motivo:** o menu não tinha um item para voltar ao começo da página — só
o logo fazia isso, e nem todo visitante percebe que o logo é clicável.

---

## 22:50 — Cria a página do blog

**O que mudou:** nova página `/blog/`, no visual das páginas legais, com
um aviso de "primeiros textos a caminho" enquanto não há posts. "Blog"
entrou no menu do topo da landing e no `sitemap-paginas.xml`, e o CSS
ganhou o bloco `.blog-vazio`.

**Arquivos:** `blog/index.html`, `index.html`, `sitemap-paginas.xml`, `styles.css`

**Commit:** `edce53a`

**Motivo:** pedido de um item "Blog" no menu, com a página criada neste
repositório.

---

## 22:50 — Menu vira hambúrguer até 1100px

**O que mudou:** o menu completo passou a virar hambúrguer em telas de até
1100px (antes era 760px), e os itens não quebram mais linha. O
`?v=` do `styles.css` subiu para `20260912` em todas as páginas.

**Arquivos:** `styles.css`, `index.html`, `termos/index.html`, `privacidade/index.html`, `cookies/index.html`

**Commit:** `edce53a`

**Motivo:** capturas em Chrome headless mostraram que entre 760px e
1100px "O que é" quebrava em três linhas e o botão "Criar meu minisitee"
saía cortado da tela — problema que já existia e piorou com "Início" e
"Blog".

---

## 22:50 — Todos os links da landing e do blog abrem em nova aba

**O que mudou:** as âncoras internas (menu, logo, rodapé) e os links
`mailto:` também ganharam `target="_blank" rel="noopener"`, na landing e
na página do blog. Só o link invisível "Pular para o conteúdo", de
acessibilidade, ficou de fora.

**Arquivos:** `index.html`, `blog/index.html`

**Commit:** `edce53a`

**Motivo:** o pedido era para todos os links abrirem em nova página; na
mudança anterior as âncoras e os e-mails tinham ficado de fora por
decisão minha.

---

## 23:08 — Artigo "Os 13 tipos de item do minisitee"

**O que mudou:** novo post em `/blog/tipos-de-item/` explicando cada tipo
de item (o que o dono preenche, o que o cliente vê e dicas), agrupados por
função, com status, limite do plano Free e dados estruturados de
BlogPosting. Entrou na lista do blog, no `sitemap-paginas.xml` e ganhou
link no hero ("Novo") e na seção de blocos.

**Arquivos:** `blog/tipos-de-item/index.html`, `blog/index.html`, `index.html`, `sitemap-paginas.xml`

**Commit:** `bf49336`

**Motivo:** pedido de um artigo explicando cada tipo de item. O conteúdo
saiu da leitura do código do app.minisitee.com (autorizada), sem prometer o
que o código não faz.

---

## 23:08 — Site refeito com Tailwind, shadcn/ui e visual do Linear

**O que mudou:** landing, blog e páginas legais passaram a usar Tailwind
CSS v4 com componentes no padrão do shadcn/ui (botão, card, badge,
accordion, input) e tokens do shadcn em `src/site.css`. O CSS gerado fica
em `assets/site.css`, commitado; o `styles.css` antigo foi removido.
Visual inspirado no Linear, com fundo branco (a primeira versão, escura,
foi trocada a pedido). Cabeçalho e rodapé novos e iguais em todas as
páginas, menu hambúrguer abaixo de 1024px, "Entrar" fora do topo (fica no
rodapé), plano Free corrigido para 5 itens e `CLAUDE.md` atualizado com o
fluxo do CSS.

**Arquivos:** `index.html`, `blog/index.html`, `termos/index.html`, `privacidade/index.html`, `cookies/index.html`, `script.js`, `src/site.css`, `assets/site.css`, `package.json`, `package-lock.json`, `styles.css` (removido), `CLAUDE.md`

**Commit:** `bf49336`

**Motivo:** pedido de usar shadcn/ui como base, Tailwind e visual do
Linear no site inteiro, mantendo o site estático (sem build na VPS).
O limite de 5 itens é o `MAX_ITENS_FREE` do app. Conferido em capturas
de tela no desktop (1280px) e no iPhone 13, sem rolagem horizontal.

---

## 23:09 — Volta a fonte grande de "Sem o minisitee" e "Com o minisitee"

**O que mudou:** os títulos das duas cartas da seção "O problema" deixaram
de ser selos pequenos (12px) e voltaram a ser títulos, agora com 22px e
peso semibold. As listas das cartas voltaram para 16px.

**Arquivos:** `index.html`, `assets/site.css`

**Commit:** `6dc4c28`

**Motivo:** a fonte grande (20px) tinha sido pedida antes, e a reforma
visual com Tailwind a reduziu por engano.
