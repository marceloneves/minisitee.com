## 13:22 — Troca o preço do plano Pro para R$ 297 por ano

**O que mudou:** o plano Pro passou de R$ 97 para R$ 297 por ano. A troca
vale para o card de preços, a conta mensal ao lado (de R$ 8,08 para
R$ 24,75 por mês), a resposta sobre cobrança nas dúvidas (texto visível e
dados estruturados), o preço nos dados estruturados da oferta e o valor
nos Termos de uso.

**Arquivos:** `index.html`, `termos/index.html`

**Commit:** `5aab05e`

**Motivo:** pedido de mudança do preço do plano Pro.

**Verificado:** no local, na porta 8001, a página inicial mostra R$ 297 e
R$ 24,75 e os Termos mostram R$ 297,00 por ano; não sobrou nenhum "97"
nem "8,08" fora do log.

---

## 13:23 — Publica o novo preço do plano Pro

**O que mudou:** push do `main` para o GitHub, que dispara o deploy
automático na VPS com o preço de R$ 297 por ano.

**Arquivos:** —

**Commit:** —

**Motivo:** pedido para publicar a troca de preço.

---

## 13:54 — Corrige o campo de endereço cortado no Safari do celular

**O que mudou:** em telas de até 900px a grade do topo era
`grid-template-columns: 1fr`, e o Safari do iPhone deixava a largura
mínima própria do input (~244px) alargar a coluna além da tela — num
iPhone SE a coluna ficava com 380px numa tela de 320px, e o `overflow:
hidden` do `.hero` cortava o campo `minisitee.com/seunome`, o título e o
selo pela direita. A coluna agora é `minmax(0, 1fr)`, como já era no
desktop, e o input ganhou `width: 100%` para não depender da largura
padrão do navegador. O link do `styles.css` nas quatro páginas passou a
ser `/styles.css?v=20260911`.

**Arquivos:** `styles.css`, `index.html`, `termos/index.html`,
`privacidade/index.html`, `cookies/index.html`

**Commit:** `97b110a`

**Motivo:** o campo continuava deformado no celular depois da correção de
08/09. Aquela correção só valia para o Chrome; a de hoje resolve o
problema que aparece no Safari. A versão na URL do CSS existe porque o
servidor manda o `styles.css` com `cache-control: max-age=604800`
(7 dias): sem ela, quem já abriu o site continuaria vendo o CSS antigo.
Ao mexer no `styles.css` de novo, troque o `?v=` nas quatro páginas.

**Verificado:** no local, na porta 8001 (a 8000 estava ocupada por outro
projeto), com Playwright emulando iPhone SE, iPhone 13, iPhone 15 Pro
Max, iPhone 13 deitado (WebKit), Galaxy S9+ e Pixel 7 (Chromium), além de
Safari e Chrome no desktop: o campo e o botão cabem na tela em todos, sem
nenhum elemento passando da borda. No desktop o layout não mudou.

---

## 13:55 — Publica a correção do campo no Safari

**O que mudou:** push do `main` para o GitHub, que dispara o deploy
automático na VPS.

**Arquivos:** —

**Commit:** —

**Motivo:** pedido para comitar e publicar tudo.

---

## 19:09 — Inclui os minisites no sitemap

**O que mudou:** o `sitemap.xml` virou um índice que aponta para
`sitemap-paginas.xml` (as quatro páginas fixas, o conteúdo que o
`sitemap.xml` tinha até agora) e para `sitemap-minisites.xml`. Este último
é gerado pelo aplicativo a partir do banco: no repositório do app, o
`app/sitemap.ts` virou a rota `app/sitemap-minisites.xml/route.ts`, e
`/sitemap.xml` saiu da lista de rotas do painel no `middleware.ts`.

**Arquivos:** `sitemap.xml`, `sitemap-paginas.xml`, `CLAUDE.md`

**Commit:** `57280de` (aqui) e `a6d584d` (no `app.minisitee.com`)

**Motivo:** os minisites nunca entravam no sitemap. O aplicativo já gerava
a lista deles, mas em `minisitee.com` o servidor entrega o arquivo quando
ele existe na pasta, e o `sitemap.xml` fixo daqui ficava na frente — a
lista boa só aparecia em `app.minisitee.com/sitemap.xml`, onde nenhum
buscador ia procurar, já que o `robots.txt` anuncia
`minisitee.com/sitemap.xml`. Escrever os endereços à mão neste repositório
não resolveria: conta criada hoje só entraria no sitemap na próxima
publicação.

**Verificado:** no app local (porta 3011), `/sitemap-minisites.xml`
respondeu 200 com `application/xml` e listou os 5 minisites que têm item
ativo, inclusive dois criados hoje; `/sitemap.xml` passou a dar 404 no app,
que é o esperado — nesse endereço quem responde é o índice daqui. Os dois
XML da landing passaram no `xmllint`, e no app o `tsc --noEmit` e o
`npm run lint` não acusaram nada.

**Atenção:** publicar o app antes da landing. Na ordem inversa, o índice
fica apontando para um `sitemap-minisites.xml` que ainda dá 404.
